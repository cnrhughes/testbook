[project]
name = "testbook"
version = "0.1.0"
description = "Interactive testing framework for Jupyter notebooks with student-friendly error messages and performance metrics."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=7.2.0",
]
